While at work, start a screen session:

$ screen
screen -S projectname

>> run stuff, simply close window or go home with
CTRL-A then D
screen -d

>> go in again
screen -r projectname


>> wanna really kill
kill -9 "number of process"

>> list all screens running
screen -ls

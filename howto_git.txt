
git init folder
cd folder
git add file

#git config --global user.name sina
git commit -m "comments"
git commit -a -m "comments"

create .gitignore 

## clone

## there is an existing folder code, for copying one do
git clone code code_exp
## change code_exp, commit the changes, etc. 
## if code_exp should replace code, then do
git code pull code_exp

## branching

## there is an existing folder code, for branching one
cd code
git branch exp ## will create another branch called "exp"
git branch

## work on exp branch one
git checkout exp ## do changes and so on

## if you want to merge them
git status ## make sure that nothing is left for commiting
git merge exp

## wanna delete exp?
git branch -d exp

## working with gitk shows graphically what happens
gitk


